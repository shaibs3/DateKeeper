# Act configuration file
# This file contains default settings for running GitHub Actions locally

# Use smaller Ubuntu image for faster runs
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Use Linux AMD64 architecture (better compatibility on M1/M2 Macs)
--container-architecture linux/amd64

# Bind the current directory (faster than copying)
--bind

# Use local secrets file if it exists
--secret-file .act.secrets

# Enable verbose output for debugging
# --verbose

# Don't pull latest images every time (faster runs)
# --pull=false
